pipeline{
	agent any
	stages{
		stage('Build'){
			steps{
				SH 'mvnclean package'
			}
			post{
				success{
					echo 'now archiving...'
					archieveArtifacts artifacts: '**/target/*.war'
				}
			}
		}
	}
}